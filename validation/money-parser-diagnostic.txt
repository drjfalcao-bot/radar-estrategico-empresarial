
===== PATTERN: Number\s*\( @ 636 =====
0';
  const TODAY = () => new Date().toISOString().slice(0, 10);
  const NOW = () => new Date().toISOString();
  const uid = (prefix='id') => `${prefix}_${Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;
  const $ = (sel, root=document) => root.querySelector(sel);
  const $$ = (sel, root=document) => [...root.querySelectorAll(sel)];
  const esc = (v='') => String(v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const num = v => Number(String(v ?? '').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,'')) || 0;
  const clamp = (n,min=0,max=100) => Math.max(min, Math.min(max, Number(n)||0));
  const brl = n => Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const pct = n => `${Number(n||0).toLocaleString('pt-BR',{maximumFractionDigits:1})}%`;
  const dateBR = v => v ? new Date(`${v}T12:00:00`).toLocaleDateString('pt-BR') : '—';
  const daysSince = v => {
    if (!v) return 0;
    const a = new Date(`${String(v).slice(0,10)}T12:00:00`);
    const b = new Date(`${TODAY()}T12:00:00`);
    return Math.max(0, Math.floor((b-a)/86400000));
  };

  const STAGES = [
    ['identificada','Identificada'],

===== PATTERN: Number\s*\( @ 790 =====
{Date.now().toString(36)}_${Math.random().toString(36).slice(2,8)}`;
  const $ = (sel, root=document) => root.querySelector(sel);
  const $$ = (sel, root=document) => [...root.querySelectorAll(sel)];
  const esc = (v='') => String(v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const num = v => Number(String(v ?? '').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,'')) || 0;
  const clamp = (n,min=0,max=100) => Math.max(min, Math.min(max, Number(n)||0));
  const brl = n => Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const pct = n => `${Number(n||0).toLocaleString('pt-BR',{maximumFractionDigits:1})}%`;
  const dateBR = v => v ? new Date(`${v}T12:00:00`).toLocaleDateString('pt-BR') : '—';
  const daysSince = v => {
    if (!v) return 0;
    const a = new Date(`${String(v).slice(0,10)}T12:00:00`);
    const b = new Date(`${TODAY()}T12:00:00`);
    return Math.max(0, Math.floor((b-a)/86400000));
  };

  const STAGES = [
    ['identificada','Identificada'],
    ['diagnostico','Diagnóstico'],
    ['aguardando_dados','Aguardando dados'],
    ['analise','Em análise'],
    ['oportunidade','Oportunidade encontrad

===== PATTERN: Number\s*\( @ 825 =====
ndom().toString(36).slice(2,8)}`;
  const $ = (sel, root=document) => root.querySelector(sel);
  const $$ = (sel, root=document) => [...root.querySelectorAll(sel)];
  const esc = (v='') => String(v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const num = v => Number(String(v ?? '').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,'')) || 0;
  const clamp = (n,min=0,max=100) => Math.max(min, Math.min(max, Number(n)||0));
  const brl = n => Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const pct = n => `${Number(n||0).toLocaleString('pt-BR',{maximumFractionDigits:1})}%`;
  const dateBR = v => v ? new Date(`${v}T12:00:00`).toLocaleDateString('pt-BR') : '—';
  const daysSince = v => {
    if (!v) return 0;
    const a = new Date(`${String(v).slice(0,10)}T12:00:00`);
    const b = new Date(`${TODAY()}T12:00:00`);
    return Math.max(0, Math.floor((b-a)/86400000));
  };

  const STAGES = [
    ['identificada','Identificada'],
    ['diagnostico','Diagnóstico'],
    ['aguardando_dados','Aguardando dados'],
    ['analise','Em análise'],
    ['oportunidade','Oportunidade encontrada'],
    ['estrategia','Estratégia 

===== PATTERN: Number\s*\( @ 919 =====

  const $$ = (sel, root=document) => [...root.querySelectorAll(sel)];
  const esc = (v='') => String(v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const num = v => Number(String(v ?? '').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,'')) || 0;
  const clamp = (n,min=0,max=100) => Math.max(min, Math.min(max, Number(n)||0));
  const brl = n => Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const pct = n => `${Number(n||0).toLocaleString('pt-BR',{maximumFractionDigits:1})}%`;
  const dateBR = v => v ? new Date(`${v}T12:00:00`).toLocaleDateString('pt-BR') : '—';
  const daysSince = v => {
    if (!v) return 0;
    const a = new Date(`${String(v).slice(0,10)}T12:00:00`);
    const b = new Date(`${TODAY()}T12:00:00`);
    return Math.max(0, Math.floor((b-a)/86400000));
  };

  const STAGES = [
    ['identificada','Identificada'],
    ['diagnostico','Diagnóstico'],
    ['aguardando_dados','Aguardando dados'],
    ['analise','Em análise'],
    ['oportunidade','Oportunidade encontrada'],
    ['estrategia','Estratégia estruturada'],
    ['apresentacao','Apresentação'],
    ['proposta','Proposta enviada'],
    [

===== PATTERN: Number\s*\( @ 19633 =====
ível.'
    };

    let best='combined';
    if(debtPGFN>0 && debtRFB===0) best='pgfn';
    if(debtRFB>0 && debtPGFN===0) best='rfb';
    if(l.impediment && debtPGFN>0) best=debtRFB>0?'combined':'pgfn';

    return {debt,inaction:{total:inactionTotal,increase:inactionTotal-debt,rate:s.inactionAnnualRate},rfb:rfbScenario,pgfn:pgfnScenario,combined,migration,best};
  }

  function pricingSuggestion(l){
    const p=state.db.settings.pricing; const passivo=totalDebt(l); const processCount=Math.max(0,Number(l.processCount)||0);
    const msInternal=passivo*(p.msPassivoRate/100);
    const reportRate=passivo<=p.reportThreshold?p.reportRateLow:p.reportRateHigh;
    const reportInternal=passivo*(reportRate/100);
    let defenseMonthly=p.defenseTier3Monthly, tier=`Mais de ${p.defenseTier2Max} processos`;
    if(processCount<=p.defenseTier1Max){defenseMonthly=p.defenseTier1Monthly;tier=`Até ${p.defenseTier1Max} processos`;}
    else if(processCount<=p.defenseTier2Max){defenseMonthly=p.defenseTier2Monthly;tier=`De ${p.defenseTier1Max+1} a ${p.defenseTier2Max} processos`;}
    return {
      ms:{internal:msInternal,oab:p.oabMsMinimum,suggested:Math.max(msInternal,p.oabMsMinimum),formula:`${pct(p.msPas

===== PATTERN: Number\s*\( @ 29298 =====
}">${esc(value||'')}</textarea>${opts.help?`<small>${opts.help}</small>`:''}</label>`;
    return `<label class="field"><span>${label}</span><input data-case-field="${key}" type="${type}" value="${esc(value??'')}" placeholder="${esc(opts.placeholder||'')}">${opts.help?`<small>${opts.help}</small>`:''}</label>`;
  }
  function moneyField(label,key,value,help=''){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-case-field="${key}" data-money value="${value?Number(value).toLocaleString('pt-BR'):''}" placeholder="0,00"></div>${help?`<small>${help}</small>`:''}</label>`;}
  function checkCard(label,key,value,help=''){return `<label class="check-card ${value?'selected':''}"><input type="checkbox" data-case-field="${key}" ${value?'checked':''}><span><strong>${label}</strong><small>${help}</small></span></label>`;}
  function sectionHead(n,title,copy){return `<div class="section-head"><span>${n}</span><div><h2>${title}</h2><p>${copy}</p></div></div>`;}

  function profileTab(l){
    return `<div class="stack">${sectionHead('01','Perfil Estratégico da Empresa','Estrutura, momento empresarial, capacidade de reação e sinais comerciais que orientam a análise.'

===== PATTERN: Number\s*\( @ 62806 =====
Year',p.oabYear)}${settingsMoney('Mínimo MS','oabMsMinimum',p.oabMsMinimum)}${settingsMoney('Mínimo laudo','oabReportMinimum',p.oabReportMinimum)}${settingsMoney('Mínimo mensal defesa','oabDefenseMonthlyMinimum',p.oabDefenseMonthlyMinimum)}${settingsArea('Observação da referência','oabNote',p.oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${

===== PATTERN: Number\s*\( @ 62947 =====
mum)}${settingsMoney('Mínimo mensal defesa','oabDefenseMonthlyMinimum',p.oabDefenseMonthlyMinimum)}${settingsArea('Observação da referência','oabNote',p.oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel

===== PATTERN: Number\s*\( @ 63020 =====
oabDefenseMonthlyMinimum)}${settingsArea('Observação da referência','oabNote',p.oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais 

===== PATTERN: Number\s*\( @ 63100 =====
oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('

===== PATTERN: Number\s*\( @ 63267 =====
v class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PG

===== PATTERN: Number\s*\( @ 63521 =====
umber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${

===== PATTERN: Number\s*\( @ 63799 =====
,'defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${sett

===== PATTERN: Number\s*\( @ 63895 =====
thly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima

===== PATTERN: Number\s*\( @ 63964 =====
','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }


===== PATTERN: Number\s*\( @ 64066 =====
nths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input dat

===== PATTERN: Number\s*\( @ 64163 =====
ass="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,valu

===== PATTERN: Number\s*\( @ 64236 =====
rizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting=

===== PATTERN: Number\s*\( @ 64329 =====
atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,ke

===== PATTERN: Number\s*\( @ 64849 =====
recido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-setting="${key}" data-setting-money value="${value?Number(value).toLocaleString('pt-BR'):''}"></div></label>`;}
  function settingsArea(label,key,value){return `<label class="field span-2"><span>${label}</span><textarea data-setting="${key}">${esc(value||'')}</textarea></label>`;}

  function labelOf(key,value){
    const maps={
      cadastralStatus:{ativa:'Ativa',inapta:'Inapta',suspensa:'Suspensa',baixada:'Baixada'

===== PATTERN: Number\s*\( @ 65186 =====
ingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-setting="${key}" data-setting-money value="${value?Number(value).toLocaleString('pt-BR'):''}"></div></label>`;}
  function settingsArea(label,key,value){return `<label class="field span-2"><span>${label}</span><textarea data-setting="${key}">${esc(value||'')}</textarea></label>`;}

  function labelOf(key,value){
    const maps={
      cadastralStatus:{ativa:'Ativa',inapta:'Inapta',suspensa:'Suspensa',baixada:'Baixada'},
      taxRegime:{nao_informado:'Não informado',simples:'Simples Nacional',presumido:'Lucro Presumido',real:'Lucro Real',mei:'MEI'},
      revenueBand:{nao_informado:'Não informado',ate_100:'Até R$ 100 mil',ate_500:'R$ 100 mil a R$ 500 mil',ate_2m:'R$ 500 mil a R$ 2 milhões',ate_10m:'R$ 2 milhões a R$ 10 milhões',acima_10m:'Acima de 

===== PATTERN: pgfnPrev @ 3807 =====
onthly:0, canSupportEntry:'nao_sei', workingCapital:'moderado', cashPressure:'moderada',
    decisionMaker:'nao_sei', problemRecognition:'medio', documentWillingness:'medio', intentToSolve:'medio', decisionHorizon:'nao_informado',
    b2bShare:50, marginLevel:'media', receivableDays:30, taxCashDependence:'moderada', priceFlexibility:'media', longContracts:'nao_sei', taxBenefits:'nao_sei', erpReadiness:'parcial', accountingReadiness:'parcial', splitReadiness:'baixa',
    rfbDebt:0, pgfnSimple:0, pgfnPrev:0, pgfnOther:0, installmentActive:false, reparcelment:'nenhum', capag:'nao_sei', impediment:false, omissions:false, certificateNeed:'media',
    execution:false, citation:false, block:false, seizure:false, expropriation:false, guarantee:false, processCount:0, exposedAssets:false, priorBlocks:false,
    stage:'identificada', status:'em_analise', createdAt:'', updatedAt:'', lastMovementAt:'', nextAction:'', nextActionDate:'', meetingDate:'', proposalSentAt:'', signatureStatus:'nao_aplicavel', lostReason:'',
    notes:[], history:[], simulations:[], selectedScenarioId:'',
    manualStrategyTitle:'', manualStrategySummary:'', manualPlan:'',
    proposal: { services:[], customDescription:'', cus

===== PATTERN: pgfnPrev @ 12749 =====
') closing=clamp(l.overrides.closingScore);
    closing=clamp(closing);

    const opportunity = String(l.overrides?.opportunityScore||'').trim()!=='' ? clamp(l.overrides.opportunityScore) : clamp(need*0.7+closing*0.3);

    return {rt:Math.round(rt),financial:Math.round(financialRisk),fiscal:Math.round(fiscalRisk),collection:Math.round(collection),need:Math.round(need),closing:Math.round(closing),opportunity:Math.round(opportunity)};
  }

  function totalPGFN(l){ return num(l.pgfnSimple)+num(l.pgfnPrev)+num(l.pgfnOther); }
  function totalDebt(l){ return num(l.rfbDebt)+totalPGFN(l); }
  function favored(l){ return ['simples','mei','me_epp','pf'].includes(l.taxRegime) || l.profile==='me_epp'; }
  function minAdjusted(principal,months,min){
    if(principal<=0) return {months:0,installment:0};
    let m=Math.max(1,Math.floor(months)); let p=principal/m;
    if(p<min){m=Math.max(1,Math.floor(principal/min)); p=principal/m;}
    return {months:m,installment:p};
  }

  function strategyEngine(l,ratings){
    const fronts=[];
    let title='Diagnóstico e organização estratégica do passivo';
    let summary='Recomenda-se consolidar as informações, validar a situação fiscal e construir cenários d

===== PATTERN: pgfnPrev @ 17340 =====
ed:s.pgfnDiscountDGeneral;
    const discount={A:s.pgfnDiscountA,B:s.pgfnDiscountB,C:s.pgfnDiscountC,D:maxDisc,nao_sei:s.pgfnDiscountC}[l.capag] ?? s.pgfnDiscountC;
    const entryRate=clamp(s.pgfnEntryRate,0,100)/100;
    const entryMonths=favored(l)?s.pgfnEntryMonthsFavored:s.pgfnEntryMonthsGeneral;
    const pgEntry=debtPGFN*entryRate;
    const pgBase=debtPGFN-pgEntry;
    const pgReduction=pgBase*clamp(discount,0,maxDisc)/100;
    const pgBalance=pgBase-pgReduction;
    const pgTerm=(num(l.pgfnPrev)>0 && num(l.pgfnSimple)+num(l.pgfnOther)===0)?60:145;
    const pgAdj=minAdjusted(pgBalance,Math.max(1,pgTerm-entryMonths),s.pgfnMinInstallment||100);
    const pgfnScenario={
      id:'pgfn',name:l.impediment?'PGFN — cenário condicionado ao impedimento':'PGFN — transação parametrizada',eligible:debtPGFN>0,
      original:debtPGFN,entry:pgEntry,entryMonths,reduction:pgReduction,total:pgEntry+pgBalance,months:pgAdj.months,installment:pgAdj.installment,discount,
      note:l.impediment?'Há impedimento informado; validar elegibilidade e manter plano alternativo.':'Estimativa condicionada à modalidade, elegibilidade, CAPAG e validação dos débitos.'
    };

    const combined={
      id:'combine

===== PATTERN: pgfnPrev @ 37469 =====
']]})}
        ${field('Preparação para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de transação','impediment',l.impediment,'Pode exigir cenário alternativo.')}
        ${checkCard('Omissões ou pendências cadast

===== PATTERN: pgfnPrev @ 37481 =====
   ${field('Preparação para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de transação','impediment',l.impediment,'Pode exigir cenário alternativo.')}
        ${checkCard('Omissões ou pendências cadastrais','omiss

===== PATTERN: pgfnOther @ 3819 =====
nSupportEntry:'nao_sei', workingCapital:'moderado', cashPressure:'moderada',
    decisionMaker:'nao_sei', problemRecognition:'medio', documentWillingness:'medio', intentToSolve:'medio', decisionHorizon:'nao_informado',
    b2bShare:50, marginLevel:'media', receivableDays:30, taxCashDependence:'moderada', priceFlexibility:'media', longContracts:'nao_sei', taxBenefits:'nao_sei', erpReadiness:'parcial', accountingReadiness:'parcial', splitReadiness:'baixa',
    rfbDebt:0, pgfnSimple:0, pgfnPrev:0, pgfnOther:0, installmentActive:false, reparcelment:'nenhum', capag:'nao_sei', impediment:false, omissions:false, certificateNeed:'media',
    execution:false, citation:false, block:false, seizure:false, expropriation:false, guarantee:false, processCount:0, exposedAssets:false, priorBlocks:false,
    stage:'identificada', status:'em_analise', createdAt:'', updatedAt:'', lastMovementAt:'', nextAction:'', nextActionDate:'', meetingDate:'', proposalSentAt:'', signatureStatus:'nao_aplicavel', lostReason:'',
    notes:[], history:[], simulations:[], selectedScenarioId:'',
    manualStrategyTitle:'', manualStrategySummary:'', manualPlan:'',
    proposal: { services:[], customDescription:'', customValue:0, p

===== PATTERN: pgfnOther @ 12765 =====
(l.overrides.closingScore);
    closing=clamp(closing);

    const opportunity = String(l.overrides?.opportunityScore||'').trim()!=='' ? clamp(l.overrides.opportunityScore) : clamp(need*0.7+closing*0.3);

    return {rt:Math.round(rt),financial:Math.round(financialRisk),fiscal:Math.round(fiscalRisk),collection:Math.round(collection),need:Math.round(need),closing:Math.round(closing),opportunity:Math.round(opportunity)};
  }

  function totalPGFN(l){ return num(l.pgfnSimple)+num(l.pgfnPrev)+num(l.pgfnOther); }
  function totalDebt(l){ return num(l.rfbDebt)+totalPGFN(l); }
  function favored(l){ return ['simples','mei','me_epp','pf'].includes(l.taxRegime) || l.profile==='me_epp'; }
  function minAdjusted(principal,months,min){
    if(principal<=0) return {months:0,installment:0};
    let m=Math.max(1,Math.floor(months)); let p=principal/m;
    if(p<min){m=Math.max(1,Math.floor(principal/min)); p=principal/m;}
    return {months:m,installment:p};
  }

  function strategyEngine(l,ratings){
    const fronts=[];
    let title='Diagnóstico e organização estratégica do passivo';
    let summary='Recomenda-se consolidar as informações, validar a situação fiscal e construir cenários de regularização c

===== PATTERN: pgfnOther @ 17379 =====
scount={A:s.pgfnDiscountA,B:s.pgfnDiscountB,C:s.pgfnDiscountC,D:maxDisc,nao_sei:s.pgfnDiscountC}[l.capag] ?? s.pgfnDiscountC;
    const entryRate=clamp(s.pgfnEntryRate,0,100)/100;
    const entryMonths=favored(l)?s.pgfnEntryMonthsFavored:s.pgfnEntryMonthsGeneral;
    const pgEntry=debtPGFN*entryRate;
    const pgBase=debtPGFN-pgEntry;
    const pgReduction=pgBase*clamp(discount,0,maxDisc)/100;
    const pgBalance=pgBase-pgReduction;
    const pgTerm=(num(l.pgfnPrev)>0 && num(l.pgfnSimple)+num(l.pgfnOther)===0)?60:145;
    const pgAdj=minAdjusted(pgBalance,Math.max(1,pgTerm-entryMonths),s.pgfnMinInstallment||100);
    const pgfnScenario={
      id:'pgfn',name:l.impediment?'PGFN — cenário condicionado ao impedimento':'PGFN — transação parametrizada',eligible:debtPGFN>0,
      original:debtPGFN,entry:pgEntry,entryMonths,reduction:pgReduction,total:pgEntry+pgBalance,months:pgAdj.months,installment:pgAdj.installment,discount,
      note:l.impediment?'Há impedimento informado; validar elegibilidade e manter plano alternativo.':'Estimativa condicionada à modalidade, elegibilidade, CAPAG e validação dos débitos.'
    };

    const combined={
      id:'combined',name:'Cenário estratégico combinado',

===== PATTERN: pgfnOther @ 37530 =====
Readiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de transação','impediment',l.impediment,'Pode exigir cenário alternativo.')}
        ${checkCard('Omissões ou pendências cadastrais','omissions',l.omissions,'Pode impedir regularização e ce

===== PATTERN: pgfnOther @ 37543 =====
splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de transação','impediment',l.impediment,'Pode exigir cenário alternativo.')}
        ${checkCard('Omissões ou pendências cadastrais','omissions',l.omissions,'Pode impedir regularização e certidão.')}
  

===== PATTERN: rfbDebt @ 3782 =====
_informado', sustainableMonthly:0, canSupportEntry:'nao_sei', workingCapital:'moderado', cashPressure:'moderada',
    decisionMaker:'nao_sei', problemRecognition:'medio', documentWillingness:'medio', intentToSolve:'medio', decisionHorizon:'nao_informado',
    b2bShare:50, marginLevel:'media', receivableDays:30, taxCashDependence:'moderada', priceFlexibility:'media', longContracts:'nao_sei', taxBenefits:'nao_sei', erpReadiness:'parcial', accountingReadiness:'parcial', splitReadiness:'baixa',
    rfbDebt:0, pgfnSimple:0, pgfnPrev:0, pgfnOther:0, installmentActive:false, reparcelment:'nenhum', capag:'nao_sei', impediment:false, omissions:false, certificateNeed:'media',
    execution:false, citation:false, block:false, seizure:false, expropriation:false, guarantee:false, processCount:0, exposedAssets:false, priorBlocks:false,
    stage:'identificada', status:'em_analise', createdAt:'', updatedAt:'', lastMovementAt:'', nextAction:'', nextActionDate:'', meetingDate:'', proposalSentAt:'', signatureStatus:'nao_aplicavel', lostReason:'',
    notes:[], history:[], simulations:[], selectedScenarioId:'',
    manualStrategyTitle:'', manualStrategySummary:'', manualPlan:'',
    proposal: { services:[],

===== PATTERN: rfbDebt @ 12817 =====
g);

    const opportunity = String(l.overrides?.opportunityScore||'').trim()!=='' ? clamp(l.overrides.opportunityScore) : clamp(need*0.7+closing*0.3);

    return {rt:Math.round(rt),financial:Math.round(financialRisk),fiscal:Math.round(fiscalRisk),collection:Math.round(collection),need:Math.round(need),closing:Math.round(closing),opportunity:Math.round(opportunity)};
  }

  function totalPGFN(l){ return num(l.pgfnSimple)+num(l.pgfnPrev)+num(l.pgfnOther); }
  function totalDebt(l){ return num(l.rfbDebt)+totalPGFN(l); }
  function favored(l){ return ['simples','mei','me_epp','pf'].includes(l.taxRegime) || l.profile==='me_epp'; }
  function minAdjusted(principal,months,min){
    if(principal<=0) return {months:0,installment:0};
    let m=Math.max(1,Math.floor(months)); let p=principal/m;
    if(p<min){m=Math.max(1,Math.floor(principal/min)); p=principal/m;}
    return {months:m,installment:p};
  }

  function strategyEngine(l,ratings){
    const fronts=[];
    let title='Diagnóstico e organização estratégica do passivo';
    let summary='Recomenda-se consolidar as informações, validar a situação fiscal e construir cenários de regularização compatíveis com a capacidade financeira da empresa.

===== PATTERN: rfbDebt @ 16272 =====
 o cenário atual, a inação e as alternativas de regularização.',
      'Definir a estratégia integrada e o cronograma de implementação.',
      'Apresentar a solução e acompanhar a decisão até a formalização.'
    ];
    return {title:l.manualStrategyTitle||title,summary:l.manualStrategySummary||summary,priority,fronts:[...new Set(fronts)],plan:l.manualPlan?l.manualPlan.split('\n').filter(Boolean):plan};
  }

  function simulate(l){
    const s=state.db.settings.pricing;
    const debtRFB=num(l.rfbDebt), debtPGFN=totalPGFN(l), debt=debtRFB+debtPGFN;
    const inactionRate=clamp(s.inactionAnnualRate,0,100)/100;
    const inactionTotal=debt*(1+inactionRate);

    const rfbAdj=minAdjusted(debtRFB,60,s.rfbMinPJ||500);
    const rfbScenario={
      id:'rfb',name:'Receita — parcelamento ordinário',eligible:debtRFB>0,
      original:debtRFB,entry:0,reduction:0,total:debtRFB,months:rfbAdj.months,installment:rfbAdj.installment,
      note:'Projeção nominal, sem desconto, sujeita à atualização aplicável.'
    };

    const maxDisc=favored(l)?s.pgfnDiscountDFavored:s.pgfnDiscountDGeneral;
    const discount={A:s.pgfnDiscountA,B:s.pgfnDiscountB,C:s.pgfnDiscountC,D:maxDisc,nao_sei:s.pgfnDiscountC}[l.c

===== PATTERN: rfbDebt @ 37336 =====
eparação contábil','accountingReadiness',l.accountingReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
        ${field('Preparação para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Imped

===== PATTERN: rfbDebt @ 37347 =====
ntábil','accountingReadiness',l.accountingReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
        ${field('Preparação para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de t

===== PATTERN: currency @ 869 =====
= (sel, root=document) => root.querySelector(sel);
  const $$ = (sel, root=document) => [...root.querySelectorAll(sel)];
  const esc = (v='') => String(v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const num = v => Number(String(v ?? '').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,'')) || 0;
  const clamp = (n,min=0,max=100) => Math.max(min, Math.min(max, Number(n)||0));
  const brl = n => Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const pct = n => `${Number(n||0).toLocaleString('pt-BR',{maximumFractionDigits:1})}%`;
  const dateBR = v => v ? new Date(`${v}T12:00:00`).toLocaleDateString('pt-BR') : '—';
  const daysSince = v => {
    if (!v) return 0;
    const a = new Date(`${String(v).slice(0,10)}T12:00:00`);
    const b = new Date(`${TODAY()}T12:00:00`);
    return Math.max(0, Math.floor((b-a)/86400000));
  };

  const STAGES = [
    ['identificada','Identificada'],
    ['diagnostico','Diagnóstico'],
    ['aguardando_dados','Aguardando dados'],
    ['analise','Em análise'],
    ['oportunidade','Oportunidade encontrada'],
    ['estrategia','Estratégia estruturada'],
    ['apresentacao','Apresenta

===== PATTERN: currency @ 879 =====
ot=document) => root.querySelector(sel);
  const $$ = (sel, root=document) => [...root.querySelectorAll(sel)];
  const esc = (v='') => String(v).replace(/[&<>'"]/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const num = v => Number(String(v ?? '').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,'')) || 0;
  const clamp = (n,min=0,max=100) => Math.max(min, Math.min(max, Number(n)||0));
  const brl = n => Number(n||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
  const pct = n => `${Number(n||0).toLocaleString('pt-BR',{maximumFractionDigits:1})}%`;
  const dateBR = v => v ? new Date(`${v}T12:00:00`).toLocaleDateString('pt-BR') : '—';
  const daysSince = v => {
    if (!v) return 0;
    const a = new Date(`${String(v).slice(0,10)}T12:00:00`);
    const b = new Date(`${TODAY()}T12:00:00`);
    return Math.max(0, Math.floor((b-a)/86400000));
  };

  const STAGES = [
    ['identificada','Identificada'],
    ['diagnostico','Diagnóstico'],
    ['aguardando_dados','Aguardando dados'],
    ['analise','Em análise'],
    ['oportunidade','Oportunidade encontrada'],
    ['estrategia','Estratégia estruturada'],
    ['apresentacao','Apresentação'],
   

===== PATTERN: money @ 29126 =====
''}</label>`;
    if(type==='textarea') return `<label class="field span-2"><span>${label}</span><textarea data-case-field="${key}" placeholder="${esc(opts.placeholder||'')}">${esc(value||'')}</textarea>${opts.help?`<small>${opts.help}</small>`:''}</label>`;
    return `<label class="field"><span>${label}</span><input data-case-field="${key}" type="${type}" value="${esc(value??'')}" placeholder="${esc(opts.placeholder||'')}">${opts.help?`<small>${opts.help}</small>`:''}</label>`;
  }
  function moneyField(label,key,value,help=''){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-case-field="${key}" data-money value="${value?Number(value).toLocaleString('pt-BR'):''}" placeholder="0,00"></div>${help?`<small>${help}</small>`:''}</label>`;}
  function checkCard(label,key,value,help=''){return `<label class="check-card ${value?'selected':''}"><input type="checkbox" data-case-field="${key}" ${value?'checked':''}><span><strong>${label}</strong><small>${help}</small></span></label>`;}
  function sectionHead(n,title,copy){return `<div class="section-head"><span>${n}</span><div><h2>${title}</h2><p>${copy}</p></div></div>`;}

  function profileTab(l){
    re

===== PATTERN: money @ 29224 =====
textarea data-case-field="${key}" placeholder="${esc(opts.placeholder||'')}">${esc(value||'')}</textarea>${opts.help?`<small>${opts.help}</small>`:''}</label>`;
    return `<label class="field"><span>${label}</span><input data-case-field="${key}" type="${type}" value="${esc(value??'')}" placeholder="${esc(opts.placeholder||'')}">${opts.help?`<small>${opts.help}</small>`:''}</label>`;
  }
  function moneyField(label,key,value,help=''){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-case-field="${key}" data-money value="${value?Number(value).toLocaleString('pt-BR'):''}" placeholder="0,00"></div>${help?`<small>${help}</small>`:''}</label>`;}
  function checkCard(label,key,value,help=''){return `<label class="check-card ${value?'selected':''}"><input type="checkbox" data-case-field="${key}" ${value?'checked':''}><span><strong>${label}</strong><small>${help}</small></span></label>`;}
  function sectionHead(n,title,copy){return `<div class="section-head"><span>${n}</span><div><h2>${title}</h2><p>${copy}</p></div></div>`;}

  function profileTab(l){
    return `<div class="stack">${sectionHead('01','Perfil Estratégico da Empresa','Estrutura, momento em

===== PATTERN: money @ 29277 =====
opts.placeholder||'')}">${esc(value||'')}</textarea>${opts.help?`<small>${opts.help}</small>`:''}</label>`;
    return `<label class="field"><span>${label}</span><input data-case-field="${key}" type="${type}" value="${esc(value??'')}" placeholder="${esc(opts.placeholder||'')}">${opts.help?`<small>${opts.help}</small>`:''}</label>`;
  }
  function moneyField(label,key,value,help=''){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-case-field="${key}" data-money value="${value?Number(value).toLocaleString('pt-BR'):''}" placeholder="0,00"></div>${help?`<small>${help}</small>`:''}</label>`;}
  function checkCard(label,key,value,help=''){return `<label class="check-card ${value?'selected':''}"><input type="checkbox" data-case-field="${key}" ${value?'checked':''}><span><strong>${label}</strong><small>${help}</small></span></label>`;}
  function sectionHead(n,title,copy){return `<div class="section-head"><span>${n}</span><div><h2>${title}</h2><p>${copy}</p></div></div>`;}

  function profileTab(l){
    return `<div class="stack">${sectionHead('01','Perfil Estratégico da Empresa','Estrutura, momento empresarial, capacidade de reação e sinais comerciais q

===== PATTERN: money @ 31445 =====
mado'],['simples','Simples Nacional'],['presumido','Lucro Presumido'],['real','Lucro Real'],['mei','MEI']]})}
        ${field('Atividade principal','activity',l.activity)}
        ${field('Faturamento mensal — faixa','revenueBand',l.revenueBand,'select',{options:[['nao_informado','Não informado'],['ate_100','Até R$ 100 mil'],['ate_500','R$ 100 mil a R$ 500 mil'],['ate_2m','R$ 500 mil a R$ 2 milhões'],['ate_10m','R$ 2 milhões a R$ 10 milhões'],['acima_10m','Acima de R$ 10 milhões']]})}
        ${moneyField('Faturamento mensal informado','revenueMonthly',l.revenueMonthly,'Opcional. Melhora a leitura financeira.')}
        ${field('Funcionários ativos','employees',l.employees,'number')}
        ${field('CNPJs no grupo','groupCompanies',l.groupCompanies,'number')}
        ${field('Anos de atividade','yearsActive',l.yearsActive,'number')}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Momento e postura</h3><span>Define o enquadramento da estratégia</span></div><div class="form-grid">
        ${field('Fase atual da empresa','businessPhase',l.businessPhase,'select',{options:[['crescimento','Crescimento e expansão'],['estavel','Operação estável'],['press

===== PATTERN: money @ 33336 =====
n class="panel form-panel internal"><div class="subhead"><h3>Capacidade de reação e viabilidade</h3><span>Uso interno — não aparece no relatório</span></div><div class="form-grid">
        ${field('Reserva para iniciar solução','cashReserve',l.cashReserve,'select',{options:[['nao_informado','Não informado'],['disponivel','Reserva disponível'],['parcial','Alguma capacidade, preservando caixa'],['depende_parcelamento','Depende de parcelamento'],['sem_reserva','Sem reserva imediata']]})}
        ${moneyField('Esforço mensal sustentável','sustainableMonthly',l.sustainableMonthly,'Referência interna para adequar solução e proposta.')}
        ${field('Suporta entrada inicial?','canSupportEntry',l.canSupportEntry,'select',{options:[['nao_sei','Não sabemos'],['sim','Sim'],['parcial','Parcialmente'],['nao','Não']]})}
        ${field('Capital de giro','workingCapital',l.workingCapital,'select',{options:[['forte','Forte'],['moderado','Moderado'],['pressionado','Pressionado'],['critico','Crítico']]})}
        ${field('Pressão atual de caixa','cashPressure',l.cashPressure,'select',{options:[['baixa','Baixa'],['moderada','Moderada'],['elevada','Elevada'],['critica','Crítica']]})}
        ${field('De

===== PATTERN: money @ 37306 =====
aixa']]})}
        ${field('Preparação contábil','accountingReadiness',l.accountingReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
        ${field('Preparação para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhad

===== PATTERN: money @ 37367 =====
eadiness',l.accountingReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
        ${field('Preparação para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de transação','impedim

===== PATTERN: money @ 37433 =====
'parcial','Parcial'],['baixa','Baixa']]})}
        ${field('Preparação para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de transação','impediment',l.impediment,'Pode exigir cenário alternativo.')}
        ${c

===== PATTERN: money @ 37502 =====
o para split payment','splitReadiness',l.splitReadiness,'select',{options:[['boa','Boa'],['parcial','Parcial'],['baixa','Baixa']]})}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Passivo Fiscal</h3><span>Fiscal Rate</span></div><div class="form-grid">
        ${moneyField('Receita Federal','rfbDebt',l.rfbDebt)}
        ${moneyField('PGFN — Simples','pgfnSimple',l.pgfnSimple)}
        ${moneyField('PGFN — Previdenciário','pgfnPrev',l.pgfnPrev)}
        ${moneyField('PGFN — Demais','pgfnOther',l.pgfnOther)}
        ${field('CAPAG','capag',l.capag,'select',{options:[['nao_sei','Não sabemos'],['A','A'],['B','B'],['C','C'],['D','D']]})}
        ${field('Necessidade de certidão','certificateNeed',l.certificateNeed,'select',{options:[['baixa','Baixa'],['media','Média'],['alta','Alta']]})}
      </div><div class="check-grid">
        ${checkCard('Parcelamento ativo','installmentActive',l.installmentActive,'Existe negociação que precisa ser validada e acompanhada.')}
        ${checkCard('Impedimento de transação','impediment',l.impediment,'Pode exigir cenário alternativo.')}
        ${checkCard('Omissões ou pendências cadastrais','omissions',l.omissions,

===== PATTERN: money @ 54428 =====
><strong>${title}</strong><span>${formula}</span><small>${detail}</small></div><b>${value?brl(value):'Definir'}</b></label>`).join('')}</div></section>
        <section class="panel form-panel"><div class="subhead"><h3>Ajustes da proposta</h3><span>Controle total dos valores</span></div><div class="form-grid">
          ${field('Descrição breve do trabalho','proposal.customDescription',l.proposal.customDescription,'textarea',{placeholder:'Descreva o escopo em linguagem objetiva.'})}
          ${moneyField('Valor adicional ou ajuste','proposal.customValue',l.proposal.customValue)}
          ${field('Forma de pagamento','proposal.paymentTerms',l.proposal.paymentTerms,'text',{placeholder:'Ex.: entrada + 6 parcelas'})}
          ${field('Validade','proposal.validity',l.proposal.validity)}
          ${field('Observações','proposal.notes',l.proposal.notes,'textarea',{placeholder:'Condições, limites do escopo e informações comerciais.'})}
        </div><div class="form-actions"><button class="btn primary" data-action="generate-proposal">Atualizar proposta</button><button class="btn secondary" data-action="print-proposal">Imprimir proposta</button></div></section></div>
        <aside class="pa

===== PATTERN: money @ 62333 =====
culo e precificação</h1><p>Ajuste percentuais, mínimos, faixas e premissas sem alterar o código.</p></div><button class="btn primary" data-action="save-settings">Salvar parâmetros</button></section>
      <section class="settings-grid"><section class="panel form-panel"><div class="subhead"><h3>Referência OAB cadastrada</h3><span>Validação manual</span></div><div class="form-grid">
        ${settingsField('Seccional','oabSection',p.oabSection)}${settingsField('Ano','oabYear',p.oabYear)}${settingsMoney('Mínimo MS','oabMsMinimum',p.oabMsMinimum)}${settingsMoney('Mínimo laudo','oabReportMinimum',p.oabReportMinimum)}${settingsMoney('Mínimo mensal defesa','oabDefenseMonthlyMinimum',p.oabDefenseMonthlyMinimum)}${settingsArea('Observação da referência','oabNote',p.oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acim

===== PATTERN: money @ 62392 =====
xas e premissas sem alterar o código.</p></div><button class="btn primary" data-action="save-settings">Salvar parâmetros</button></section>
      <section class="settings-grid"><section class="panel form-panel"><div class="subhead"><h3>Referência OAB cadastrada</h3><span>Validação manual</span></div><div class="form-grid">
        ${settingsField('Seccional','oabSection',p.oabSection)}${settingsField('Ano','oabYear',p.oabYear)}${settingsMoney('Mínimo MS','oabMsMinimum',p.oabMsMinimum)}${settingsMoney('Mínimo laudo','oabReportMinimum',p.oabReportMinimum)}${settingsMoney('Mínimo mensal defesa','oabDefenseMonthlyMinimum',p.oabDefenseMonthlyMinimum)}${settingsArea('Observação da referência','oabNote',p.oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${setti

===== PATTERN: money @ 62462 =====
ary" data-action="save-settings">Salvar parâmetros</button></section>
      <section class="settings-grid"><section class="panel form-panel"><div class="subhead"><h3>Referência OAB cadastrada</h3><span>Validação manual</span></div><div class="form-grid">
        ${settingsField('Seccional','oabSection',p.oabSection)}${settingsField('Ano','oabYear',p.oabYear)}${settingsMoney('Mínimo MS','oabMsMinimum',p.oabMsMinimum)}${settingsMoney('Mínimo laudo','oabReportMinimum',p.oabReportMinimum)}${settingsMoney('Mínimo mensal defesa','oabDefenseMonthlyMinimum',p.oabDefenseMonthlyMinimum)}${settingsArea('Observação da referência','oabNote',p.oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Ma

===== PATTERN: money @ 62876 =====
imum)}${settingsMoney('Mínimo laudo','oabReportMinimum',p.oabReportMinimum)}${settingsMoney('Mínimo mensal defesa','oabDefenseMonthlyMinimum',p.oabDefenseMonthlyMinimum)}${settingsArea('Observação da referência','oabNote',p.oabNote)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defense

===== PATTERN: money @ 63180 =====
ass="subhead"><h3>Regras internas</h3><span>Todos os vetores ajustáveis</span></div><div class="form-grid">
        ${settingsNumber('MS — % do passivo','msPassivoRate',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}

===== PATTERN: money @ 63347 =====
',p.msPassivoRate)}${settingsMoney('Limite do laudo','reportThreshold',p.reportThreshold)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavor

===== PATTERN: money @ 63434 =====
d)}${settingsNumber('Laudo até limite — %','reportRateLow',p.reportRateLow)}${settingsNumber('Laudo acima do limite — %','reportRateHigh',p.reportRateHigh)}${settingsNumber('Defesa — máximo faixa 1','defenseTier1Max',p.defenseTier1Max)}${settingsMoney('Defesa — mensal faixa 1','defenseTier1Monthly',p.defenseTier1Monthly)}${settingsNumber('Defesa — máximo faixa 2','defenseTier2Max',p.defenseTier2Max)}${settingsMoney('Defesa — mensal faixa 2','defenseTier2Monthly',p.defenseTier2Monthly)}${settingsMoney('Defesa — mensal faixa 3','defenseTier3Monthly',p.defenseTier3Monthly)}${settingsNumber('Defesa — meses mínimos','defenseMinMonths',p.defenseMinMonths)}
      </div></section>
      <section class="panel form-panel"><div class="subhead"><h3>Premissas das simulações</h3><span>Gerenciais e parametrizáveis</span></div><div class="form-grid">
        ${settingsNumber('Agravamento anual sem atuação — %','inactionAnnualRate',p.inactionAnnualRate)}${settingsNumber('PGFN — entrada %','pgfnEntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryM

===== PATTERN: money @ 64427 =====
ntryRate',p.pgfnEntryRate)}${settingsNumber('PGFN — meses entrada favorecido','pgfnEntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input da

===== PATTERN: money @ 64510 =====
EntryMonthsFavored',p.pgfnEntryMonthsFavored)}${settingsNumber('PGFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-setting="${key}" data-setting-money value="${value?Number(value).toLocaleString(

===== PATTERN: money @ 64575 =====
GFN — meses entrada geral','pgfnEntryMonthsGeneral',p.pgfnEntryMonthsGeneral)}${settingsNumber('CAPAG C — desconto %','pgfnDiscountC',p.pgfnDiscountC)}${settingsNumber('CAPAG D geral — desconto %','pgfnDiscountDGeneral',p.pgfnDiscountDGeneral)}${settingsNumber('CAPAG D favorecido — desconto %','pgfnDiscountDFavored',p.pgfnDiscountDFavored)}${settingsMoney('PGFN — parcela mínima','pgfnMinInstallment',p.pgfnMinInstallment)}${settingsMoney('RFB — parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-setting="${key}" data-setting-money value="${value?Number(value).toLocaleString('pt-BR'):''}"></div></label>`;}
  function settingsArea(label,key

===== PATTERN: money @ 65022 =====
 parcela mínima PJ','rfbMinPJ',p.rfbMinPJ)}${settingsMoney('RFB — parcela mínima PF','rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-setting="${key}" data-setting-money value="${value?Number(value).toLocaleString('pt-BR'):''}"></div></label>`;}
  function settingsArea(label,key,value){return `<label class="field span-2"><span>${label}</span><textarea data-setting="${key}">${esc(value||'')}</textarea></label>`;}

  function labelOf(key,value){
    const maps={
      cadastralStatus:{ativa:'Ativa',inapta:'Inapta',suspensa:'Suspensa',baixada:'Baixada'},
      taxRegime:{nao_informado:'Não informado',simples:'Simples Nacional',presumido:'Lucro Presumido',real:'Lucro Real',mei:'MEI'},
      revenueBand:{nao_informado:'Nã

===== PATTERN: money @ 65107 =====
'rfbMinPF',p.rfbMinPF)}
      </div></section></section>`);
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-setting="${key}" data-setting-money value="${value?Number(value).toLocaleString('pt-BR'):''}"></div></label>`;}
  function settingsArea(label,key,value){return `<label class="field span-2"><span>${label}</span><textarea data-setting="${key}">${esc(value||'')}</textarea></label>`;}

  function labelOf(key,value){
    const maps={
      cadastralStatus:{ativa:'Ativa',inapta:'Inapta',suspensa:'Suspensa',baixada:'Baixada'},
      taxRegime:{nao_informado:'Não informado',simples:'Simples Nacional',presumido:'Lucro Presumido',real:'Lucro Real',mei:'MEI'},
      revenueBand:{nao_informado:'Não informado',ate_100:'Até R$ 100 mil',ate_500:'R$ 100 mil a R$ 500 mil',ate_2m:'R$ 50

===== PATTERN: money @ 65165 =====
;
  }
  function settingsField(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" value="${esc(value??'')}"></label>`;}
  function settingsNumber(label,key,value){return `<label class="field"><span>${label}</span><input data-setting="${key}" type="number" value="${esc(value??0)}"></label>`;}
  function settingsMoney(label,key,value){return `<label class="field"><span>${label}</span><div class="money"><i>R$</i><input data-setting="${key}" data-setting-money value="${value?Number(value).toLocaleString('pt-BR'):''}"></div></label>`;}
  function settingsArea(label,key,value){return `<label class="field span-2"><span>${label}</span><textarea data-setting="${key}">${esc(value||'')}</textarea></label>`;}

  function labelOf(key,value){
    const maps={
      cadastralStatus:{ativa:'Ativa',inapta:'Inapta',suspensa:'Suspensa',baixada:'Baixada'},
      taxRegime:{nao_informado:'Não informado',simples:'Simples Nacional',presumido:'Lucro Presumido',real:'Lucro Real',mei:'MEI'},
      revenueBand:{nao_informado:'Não informado',ate_100:'Até R$ 100 mil',ate_500:'R$ 100 mil a R$ 500 mil',ate_2m:'R$ 500 mil a R$ 2 milhões',ate_10m:'R$ 2 milhões a R$ 10 milhõe

===== PATTERN: money @ 67939 =====
e-tab]').forEach(el=>el.addEventListener('click',()=>{state.caseTab=el.dataset.caseTab;render();}));
    $$('[data-next-tab]').forEach(el=>el.addEventListener('click',()=>{state.caseTab=el.dataset.nextTab;render();window.scrollTo({top:0,behavior:'smooth'});}));
    $$('[data-case-field]').forEach(el=>{
      const event=el.type==='checkbox'?'change':'input';
      el.addEventListener(event,()=>{
        const l=current();if(!l)return;
        let value=el.type==='checkbox'?el.checked:el.dataset.money!==undefined?num(el.value):el.type==='number'?num(el.value):el.value;
        setNested(l,el.dataset.caseField,value); l.updatedAt=TODAY(); saveDB();
        if(el.type==='checkbox') el.closest('.check-card')?.classList.toggle('selected',el.checked);
        if(el.dataset.caseField==='stage'){touch(l,`Etapa alterada para ${STAGE_LABEL[value]}`,'Movimentação registrada pelo seletor do caso.');saveDB();}
      });
      if(el.dataset.money!==undefined) el.addEventListener('blur',()=>{el.value=num(el.value)?num(el.value).toLocaleString('pt-BR'):'';});
    });
    $$('[data-proposal-service]').forEach(el=>el.addEventListener('change',()=>{
      const l=current(); if(!l)return;
      const set=n

===== PATTERN: money @ 68380 =====
      let value=el.type==='checkbox'?el.checked:el.dataset.money!==undefined?num(el.value):el.type==='number'?num(el.value):el.value;
        setNested(l,el.dataset.caseField,value); l.updatedAt=TODAY(); saveDB();
        if(el.type==='checkbox') el.closest('.check-card')?.classList.toggle('selected',el.checked);
        if(el.dataset.caseField==='stage'){touch(l,`Etapa alterada para ${STAGE_LABEL[value]}`,'Movimentação registrada pelo seletor do caso.');saveDB();}
      });
      if(el.dataset.money!==undefined) el.addEventListener('blur',()=>{el.value=num(el.value)?num(el.value).toLocaleString('pt-BR'):'';});
    });
    $$('[data-proposal-service]').forEach(el=>el.addEventListener('change',()=>{
      const l=current(); if(!l)return;
      const set=new Set(l.proposal.services||[]); el.checked?set.add(el.dataset.proposalService):set.delete(el.dataset.proposalService); l.proposal.services=[...set]; l.updatedAt=TODAY();saveDB();render();
    }));
    const noteForm=$('#note-form'); if(noteForm) noteForm.addEventListener('submit',saveNote);
    const ns=$('#note-search');if(ns)ns.addEventListener('input',e=>{state.noteSearch=e.target.value;render();setTimeout(()=>$('#note-search')?.focu

===== PATTERN: money @ 70671 =====
hot')saveSnapshot();
    if(action==='refresh-report')render();
    if(action==='print-report')printOnly('report-document');
    if(action==='generate-proposal'){const l=current();l.proposal.generatedAt=NOW();touch(l,'Proposta financeira atualizada','Valores e escopo revisados no gerador.');saveDB();render();toast('Proposta atualizada.');}
    if(action==='print-proposal')printOnly('proposal-document');
  }
  function saveSettings(){
    $$('[data-setting]').forEach(el=>{let v=el.dataset.settingMoney!==undefined?num(el.value):el.type==='number'?num(el.value):el.value;state.db.settings.pricing[el.dataset.setting]=v;});saveDB();toast('Parâmetros salvos.');render();
  }
  function saveSnapshot(){
    const l=current(); if(!l)return; const sim=simulate(l),r=calculateRatings(l);
    l.simulations.push({id:uid('sim'),date:NOW(),ratings:r,simulation:sim});touch(l,'Cenário salvo',`Fotografia registrada com necessidade ${r.need}/100.`);saveDB();toast('Fotografia do cenário salva.');
  }
  function saveNote(e){
    e.preventDefault(); const l=current();if(!l)return;const fd=new FormData(e.currentTarget);const tags=fd.getAll('tags');const note={id:uid('note'),date:fd.get('date')||TODAY(),title:Str

===== PATTERN: valor @ 40401 =====
 nos Parâmetros.')}
      <section class="score-dashboard">${ratingCard('RT-Score',r.rt,'Exposição à Reforma')}${ratingCard('Financial Rate',r.financial,'Risco de caixa')}${ratingCard('Fiscal Rate',r.fiscal,'Passivo e regularidade')}${ratingCard('Collection Rate',r.collection,'Cobrança e execução')}</section>
      <section class="panel scenario-highlight"><div><div class="eyebrow">Melhor cenário preliminar</div><h2>${esc(best?.name||'Análise insuficiente')}</h2><p>${esc(best?.note||'Informe os valores do passivo para gerar a comparação.')}</p></div><div class="scenario-highlight-value"><small>Potencial estimado de redução</small><strong>${brl(best?.reduction||0)}</strong><span>Condicionado à elegibilidade e validação</span></div></section>
      <section class="scenario-grid">
        <article class="scenario-card current"><span>Cenário atual</span><h3>${brl(sim.debt)}</h3><p>Passivo informado hoje, antes de atualização e sem considerar medidas futuras.</p><ul><li>Parcela Receita estimada: ${brl(sim.rfb.installment)}</li><li>Parcela PGFN estimada: ${brl(sim.pgfn.installment)}</li><li>Risco de cobrança: ${ratingLabel(r.collection)}</li></ul></article>
        <article class="scenario-ca

===== PATTERN: valor @ 41959 =====
ocessuais':'Risco de evolução para cobrança mais gravosa'}</li></ul></article>
        <article class="scenario-card strategy"><span>Com estratégia</span><h3>${brl(best?.total||0)}</h3><p>${esc(strat.summary)}</p><ul><li>Entrada estimada: ${brl(best?.entry||0)}</li><li>Redução potencial: ${brl(best?.reduction||0)}</li><li>Estrutura compatível com análise integrada</li></ul></article>
      </section>
      <section class="panel"><div class="panel-head"><div><h2>Comparativo das simulações</h2><p>Valores nominais e preliminares.</p></div><button class="btn secondary small" data-action="save-snapshot">Salvar fotografia do cenário</button></div><div class="table-wrap"><table><thead><tr><th>Cenário</th><th>Valor original</th><th>Entrada</th><th>Redução</th><th>Prazo / parcela</th><th>Observação</th></tr></thead><tbody>
        ${scenarioRow(sim.rfb)}${scenarioRow(sim.pgfn)}${scenarioRow(sim.migration)}
      </tbody></table></div></section>
      <section class="panel details"><details><summary>Ajustar premissas avançadas</summary><p>Os percentuais, mínimos e regras de cálculo ficam centralizados na tela <strong>Parâmetros</strong>. Isso evita alterar código para cada política comercial ou m

===== PATTERN: valor @ 42170 =====
p><ul><li>Entrada estimada: ${brl(best?.entry||0)}</li><li>Redução potencial: ${brl(best?.reduction||0)}</li><li>Estrutura compatível com análise integrada</li></ul></article>
      </section>
      <section class="panel"><div class="panel-head"><div><h2>Comparativo das simulações</h2><p>Valores nominais e preliminares.</p></div><button class="btn secondary small" data-action="save-snapshot">Salvar fotografia do cenário</button></div><div class="table-wrap"><table><thead><tr><th>Cenário</th><th>Valor original</th><th>Entrada</th><th>Redução</th><th>Prazo / parcela</th><th>Observação</th></tr></thead><tbody>
        ${scenarioRow(sim.rfb)}${scenarioRow(sim.pgfn)}${scenarioRow(sim.migration)}
      </tbody></table></div></section>
      <section class="panel details"><details><summary>Ajustar premissas avançadas</summary><p>Os percentuais, mínimos e regras de cálculo ficam centralizados na tela <strong>Parâmetros</strong>. Isso evita alterar código para cada política comercial ou modalidade.</p><button class="btn secondary" data-nav="settings">Abrir parâmetros</button></details></section>
      <div class="form-actions"><button class="btn primary" data-next-tab="estrategia">Construir estr

===== PATTERN: valor @ 46201 =====
ão';
    return 'Cenário empresarial ainda em consolidação';
  }
  function problemSummary(l,r){ return `Necessidade estratégica ${ratingLabel(r.need).toLowerCase()}, com RT-Score ${r.rt}, Financial Rate ${r.financial}, Fiscal Rate ${r.fiscal} e Collection Rate ${r.collection}.`; }

  function reportTab(l,r){
    const sim=simulate(l), strat=strategyEngine(l,r), best=sim[sim.best];
    return `<div class="stack">${sectionHead('05','Relatório Estratégico Empresarial','Documento profissional, sem valores de honorários e sem exposição excessiva das teses.')}
      <div class="report-actions"><button class="btn secondary" data-action="refresh-report">Atualizar leitura</button><button class="btn primary" data-action="print-report">Imprimir / salvar PDF</button></div>
      <article class="report" id="report-document"><header class="report-cover"><div class="report-brand"><div class="brand-mark">RE</div><div><strong>RADAR ESTRATÉGICO EMPRESARIAL</strong><span>Relatório de análise e indicação estratégica</span></div></div><div><small>EMPRESA ANALISADA</small><h1>${esc(l.companyName||'Empresa não informada')}</h1><p>${esc(l.cnpj||'CNPJ não informado')}</p></div><div class="report-date">Emitido 

===== PATTERN: valor @ 52957 =====
ice.report.suggested,price.report.formula,`Referência interna ${brl(price.report.internal)}${price.report.oab?` · mínimo OAB cadastrado ${brl(price.report.oab)}`:''}`],
      ['defesa','Gestão de passivo com defesa judicial',price.defense.monthly,`${price.defense.tier} · mínimo ${price.defense.minMonths} meses`,`Mensalidade sugerida ${brl(price.defense.monthly)} · total mínimo ${brl(price.defense.total)}`],
      ['negociacao','Negociação e regularização do passivo',0,'Definir conforme escopo','Valor livre'],
      ['reforma','Projeto de adequação à Reforma Tributária',0,'Definir conforme escopo','Valor livre'],
      ['garantia','Estruturação de garantia',0,'Definir conforme escopo','Valor livre']
    ];
    const selectedTotal=services.filter(x=>selected.has(x[0]) && x[0]!=='defesa').reduce((a,x)=>a+x[2],0)+ (selected.has('defesa')?price.defense.total:0)+num(l.proposal.customValue);
    return `<div class="stack">${sectionHead('06','Gerador de Proposta Financeira','Selecione o trabalho, revise a referência interna e ajuste livremente os valores e condições.')}
      <section class="proposal-layout"><div class="stack"><section class="panel"><div class="subhead"><h3>Serviços e referênci

===== PATTERN: valor @ 53062 =====
.oab?` · mínimo OAB cadastrado ${brl(price.report.oab)}`:''}`],
      ['defesa','Gestão de passivo com defesa judicial',price.defense.monthly,`${price.defense.tier} · mínimo ${price.defense.minMonths} meses`,`Mensalidade sugerida ${brl(price.defense.monthly)} · total mínimo ${brl(price.defense.total)}`],
      ['negociacao','Negociação e regularização do passivo',0,'Definir conforme escopo','Valor livre'],
      ['reforma','Projeto de adequação à Reforma Tributária',0,'Definir conforme escopo','Valor livre'],
      ['garantia','Estruturação de garantia',0,'Definir conforme escopo','Valor livre']
    ];
    const selectedTotal=services.filter(x=>selected.has(x[0]) && x[0]!=='defesa').reduce((a,x)=>a+x[2],0)+ (selected.has('defesa')?price.defense.total:0)+num(l.proposal.customValue);
    return `<div class="stack">${sectionHead('06','Gerador de Proposta Financeira','Selecione o trabalho, revise a referência interna e ajuste livremente os valores e condições.')}
      <section class="proposal-layout"><div class="stack"><section class="panel"><div class="subhead"><h3>Serviços e referências</h3><span>Uso interno</span></div><div class="service-list">${services.map(([id,title,value,formula,de

===== PATTERN: valor @ 53151 =====
e passivo com defesa judicial',price.defense.monthly,`${price.defense.tier} · mínimo ${price.defense.minMonths} meses`,`Mensalidade sugerida ${brl(price.defense.monthly)} · total mínimo ${brl(price.defense.total)}`],
      ['negociacao','Negociação e regularização do passivo',0,'Definir conforme escopo','Valor livre'],
      ['reforma','Projeto de adequação à Reforma Tributária',0,'Definir conforme escopo','Valor livre'],
      ['garantia','Estruturação de garantia',0,'Definir conforme escopo','Valor livre']
    ];
    const selectedTotal=services.filter(x=>selected.has(x[0]) && x[0]!=='defesa').reduce((a,x)=>a+x[2],0)+ (selected.has('defesa')?price.defense.total:0)+num(l.proposal.customValue);
    return `<div class="stack">${sectionHead('06','Gerador de Proposta Financeira','Selecione o trabalho, revise a referência interna e ajuste livremente os valores e condições.')}
      <section class="proposal-layout"><div class="stack"><section class="panel"><div class="subhead"><h3>Serviços e referências</h3><span>Uso interno</span></div><div class="service-list">${services.map(([id,title,value,formula,detail])=>`<label class="service-item ${selected.has(id)?'selected':''}"><input type="check

===== PATTERN: valor @ 53512 =====
 Reforma Tributária',0,'Definir conforme escopo','Valor livre'],
      ['garantia','Estruturação de garantia',0,'Definir conforme escopo','Valor livre']
    ];
    const selectedTotal=services.filter(x=>selected.has(x[0]) && x[0]!=='defesa').reduce((a,x)=>a+x[2],0)+ (selected.has('defesa')?price.defense.total:0)+num(l.proposal.customValue);
    return `<div class="stack">${sectionHead('06','Gerador de Proposta Financeira','Selecione o trabalho, revise a referência interna e ajuste livremente os valores e condições.')}
      <section class="proposal-layout"><div class="stack"><section class="panel"><div class="subhead"><h3>Serviços e referências</h3><span>Uso interno</span></div><div class="service-list">${services.map(([id,title,value,formula,detail])=>`<label class="service-item ${selected.has(id)?'selected':''}"><input type="checkbox" data-proposal-service="${id}" ${selected.has(id)?'checked':''}><div><strong>${title}</strong><span>${formula}</span><small>${detail}</small></div><b>${value?brl(value):'Definir'}</b></label>`).join('')}</div></section>
        <section class="panel form-panel"><div class="subhead"><h3>Ajustes da proposta</h3><span>Controle total dos valores</span></div><

===== PATTERN: valor @ 54196 =====
iv><div class="service-list">${services.map(([id,title,value,formula,detail])=>`<label class="service-item ${selected.has(id)?'selected':''}"><input type="checkbox" data-proposal-service="${id}" ${selected.has(id)?'checked':''}><div><strong>${title}</strong><span>${formula}</span><small>${detail}</small></div><b>${value?brl(value):'Definir'}</b></label>`).join('')}</div></section>
        <section class="panel form-panel"><div class="subhead"><h3>Ajustes da proposta</h3><span>Controle total dos valores</span></div><div class="form-grid">
          ${field('Descrição breve do trabalho','proposal.customDescription',l.proposal.customDescription,'textarea',{placeholder:'Descreva o escopo em linguagem objetiva.'})}
          ${moneyField('Valor adicional ou ajuste','proposal.customValue',l.proposal.customValue)}
          ${field('Forma de pagamento','proposal.paymentTerms',l.proposal.paymentTerms,'text',{placeholder:'Ex.: entrada + 6 parcelas'})}
          ${field('Validade','proposal.validity',l.proposal.validity)}
          ${field('Observações','proposal.notes',l.proposal.notes,'textarea',{placeholder:'Condições, limites do escopo e informações comerciais.'})}
        </div><div class="f

===== PATTERN: valor @ 54440 =====
itle}</strong><span>${formula}</span><small>${detail}</small></div><b>${value?brl(value):'Definir'}</b></label>`).join('')}</div></section>
        <section class="panel form-panel"><div class="subhead"><h3>Ajustes da proposta</h3><span>Controle total dos valores</span></div><div class="form-grid">
          ${field('Descrição breve do trabalho','proposal.customDescription',l.proposal.customDescription,'textarea',{placeholder:'Descreva o escopo em linguagem objetiva.'})}
          ${moneyField('Valor adicional ou ajuste','proposal.customValue',l.proposal.customValue)}
          ${field('Forma de pagamento','proposal.paymentTerms',l.proposal.paymentTerms,'text',{placeholder:'Ex.: entrada + 6 parcelas'})}
          ${field('Validade','proposal.validity',l.proposal.validity)}
          ${field('Observações','proposal.notes',l.proposal.notes,'textarea',{placeholder:'Condições, limites do escopo e informações comerciais.'})}
        </div><div class="form-actions"><button class="btn primary" data-action="generate-proposal">Atualizar proposta</button><button class="btn secondary" data-action="print-proposal">Imprimir proposta</button></div></section></div>
        <aside class="panel proposal

===== PATTERN: valor @ 55232 =====
  ${field('Observações','proposal.notes',l.proposal.notes,'textarea',{placeholder:'Condições, limites do escopo e informações comerciais.'})}
        </div><div class="form-actions"><button class="btn primary" data-action="generate-proposal">Atualizar proposta</button><button class="btn secondary" data-action="print-proposal">Imprimir proposta</button></div></section></div>
        <aside class="panel proposal-summary"><div class="eyebrow">Resumo financeiro</div><h2>${brl(selectedTotal)}</h2><p>Valor total mínimo estimado com base nos serviços selecionados e nos parâmetros cadastrados.</p><div class="proposal-lines">${services.filter(x=>selected.has(x[0])).map(x=>`<div><span>${x[1]}</span><strong>${x[0]==='defesa'?`${brl(price.defense.monthly)}/mês`:x[2]?brl(x[2]):'A definir'}</strong></div>`).join('')}${num(l.proposal.customValue)?`<div><span>Ajuste adicional</span><strong>${brl(l.proposal.customValue)}</strong></div>`:''}</div><div class="reference-note"><strong>${esc(state.db.settings.pricing.oabSection)}</strong><br>${esc(state.db.settings.pricing.oabYear||'Ano não cadastrado')} · ${esc(state.db.settings.pricing.oabNote)}</div></aside>
      </section>
      ${proposalDocument(l,ser

===== PATTERN: valor @ 70422 =====
out'){localStorage.removeItem(SESSION_KEY);state.session=null;render();}
    if(action==='dashboard'){state.view='dashboard';render();}
    if(action==='new-case')newCase();
    if(action==='save-settings')saveSettings();
    if(action==='save-snapshot')saveSnapshot();
    if(action==='refresh-report')render();
    if(action==='print-report')printOnly('report-document');
    if(action==='generate-proposal'){const l=current();l.proposal.generatedAt=NOW();touch(l,'Proposta financeira atualizada','Valores e escopo revisados no gerador.');saveDB();render();toast('Proposta atualizada.');}
    if(action==='print-proposal')printOnly('proposal-document');
  }
  function saveSettings(){
    $$('[data-setting]').forEach(el=>{let v=el.dataset.settingMoney!==undefined?num(el.value):el.type==='number'?num(el.value):el.value;state.db.settings.pricing[el.dataset.setting]=v;});saveDB();toast('Parâmetros salvos.');render();
  }
  function saveSnapshot(){
    const l=current(); if(!l)return; const sim=simulate(l),r=calculateRatings(l);
    l.simulations.push({id:uid('sim'),date:NOW(),ratings:r,simulation:sim});touch(l,'Cenário salvo',`Fotografia registrada com necessidade ${r.need}/100.`);saveDB();toast(
